<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_search"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/activity_background_color">

    <RelativeLayout
        android:id="@+id/logoLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="20dp" >

        <include
            layout="@layout/logo_layout" />

    </RelativeLayout>

    <TextView
        android:id="@+id/searchTitleTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/logoLayout"
        android:text="@string/search_movies_text"
        android:textStyle="italic|bold"
        android:textSize="20sp"
        android:textColor="@color/movie_dark_purple"
        android:gravity="center"
        android:layout_marginTop="25dp"/>

    <View
        android:id="@+id/lineSeparatorView"
        android:layout_below="@+id/searchTitleTextView"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@color/movie_dark_purple"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp" />

    <TableLayout
        android:layout_below="@+id/lineSeparatorView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:padding="10dp">

        <TableRow>

            <TextView
                style="@style/ReleaseDateTextView"/>

            <Button
                android:id="@+id/datePickerButtonView"
                style="@style/ReleaseDateButtonView"/>

        </TableRow>

        <TableRow
            android:layout_marginTop="@dimen/search_act_row_margin">

            <TextView
                style="@style/GenreTextView"/>

            <Spinner
                android:id="@+id/genreSpinnerView"
                style="@style/GenreSpinnerView"/>
        </TableRow>

        <TableRow
            android:layout_marginTop="@dimen/search_act_row_margin" >

            <TextView
                style="@style/RatingTextView"/>

            <RatingBar
                android:id="@+id/ratingBarView"
                style="@style/RatingBarView"/>
        </TableRow>


        <TableRow
            android:layout_marginTop="@dimen/search_act_row_margin">

            <TextView
                style="@style/VoteCountTextView" />

            <EditText
                android:id="@+id/voteCountEditTextView"
                style="@style/VoteCountEditTextView"/>
        </TableRow>

    </TableLayout>

</RelativeLayout>
